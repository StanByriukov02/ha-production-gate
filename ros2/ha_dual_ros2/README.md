# ha_dual_ros2 (T5)

Thin ROS2 insert: `robot_description` / URDF → HA Dual socket.

- Bridge (no rclpy): `ha-dual-ros2` from repo root after `pip install -e .`
- Package: this directory — `colcon build --packages-select ha_dual_ros2`
- Docs: [`../../docs/examples/10_thin_ros2_dual.md`](../../docs/examples/10_thin_ros2_dual.md)

Not a Gazebo stack. Soft teaching Dual · not MEASURED.
