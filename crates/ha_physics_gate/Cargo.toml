[package]
name = "ha_physics_gate"
version = "0.1.0"
edition = "2021"
description = "H6 Physics gate / apoptosis — non-Python core"

[lib]
path = "src/lib.rs"

[[bin]]
name = "ha-physics-gate"
path = "src/main.rs"

[dependencies]
serde_json = "1"
clap = { version = "4", features = ["derive"] }
