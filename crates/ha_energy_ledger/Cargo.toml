[package]
name = "ha_energy_ledger"
version = "0.1.0"
edition = "2021"
description = "H7 Energy Ledger / kinetic tax — non-Python core"

[lib]
path = "src/lib.rs"

[[bin]]
name = "ha-energy-ledger"
path = "src/main.rs"

[dependencies]
serde_json = "1"
clap = { version = "4", features = ["derive"] }
